name: qore_server_postgres
description: Server command line app for Q-ORE.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.0

# Add regular dependencies here.
dependencies:
  characters: ^1.3.0
  logger: ^2.0.2
  openid_client: ^0.4.7
  postgres: ^2.6.2
  web_socket_channel: ^2.4.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
